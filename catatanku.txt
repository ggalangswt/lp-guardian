
  🟢 A. BE Data — polish opsional (domain-mu)

  1. Auth di /tee/sign (keamanan) — endpoint CVM sekarang publik tanpa auth. tee-attestor lama punya pola
  AUTH_TOKEN Bearer. Effort: kecil. Value: sedang (CVM short-lived, tapi rapi).
  2. Anchor attestation hash ke Mantle — sekarang attestationHash (VERIFIED) cuma masuk ke message, belum 
  benar-benar ditulis ke LPGuardianTuringRegistry. Ini justru headline hackathon Turing (benchmark AI on-chain).
  Wiring recordOutcome/anchor pakai attestation hash. Effort: sedang. Value: TINGGI.
  3. Quote verification — verifikasi TDX quote (bukan cuma simpan hash), pakai logika verdict.mjs/verifier.
  Effort: sedang. Value: sedang.
  4. Bersih-bersih Bybit — karena sekarang pakai priceHistory dari Node, kode Bybit di CVM jadi fallback yang tak
  terpakai. Bisa disederhanakan/hapus. Effort: kecil. Value: rendah.
  5. Merchant Moe subgraph (Python-side) — dulu ditandai opsional; enrichment posisi dari Python. Effort: sedang. 
  Value: rendah (BE Agent sudah handle scanning).

  🟡 B. Yang menggate demo end-to-end

  6. Demo wallet Mantle dengan posisi LP — (kamu bilang nanti) wajib untuk full-flow scan→correlate→optimize jalan
  & tampilkan correlation asli + VERIFIED.
  7. .env lokal — yang kubuat memuat config Mantle penuh → langkah diagnose lokal coba operasi on-chain nyata.
  Perlu diputuskan: trim ke minimal (cuma BE_DATA_SERVICE_URL) untuk dev, atau biarkan.
  8. Investigasi langkah simulate/diagnose saat full config (kenapa optimize gagal lokal) — domain BE Agent, tapi 
  aku bisa bantu telusuri.

  🔵 C. Lintas-tim (submission, bukan domain BE Data)

  9. FE: demo video (<3 menit), pitch deck
  10. SC: verifikasi kontrak di Mantle explorer, smoke test forge
  11. BE Agent: pastikan pipeline robust + Byreal Skills callable
  12. Submission checklist (di TASK.md): typecheck mcp, env production, README final

  ---
  Lalu (#6) demo wallet saat kamu siap, untuk full-flow.




  INI YANG BELUM DAN PERLU DILAKUKAN UNTUK PRODUCTION-READY: 

    ⚠️  Untuk mengaktifkan di production (aksimu)

  ┌─────────┬─────────────────────────────────────────────────────────────────────────────────────────────────┐
  │  Item   │                                        Yang perlu di-set                                        │
  ├─────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ #1 auth │ Set BE_DATA_AUTH_TOKEN (secret) di CVM Phala (redeploy) dan nilai sama di Railway. Sekarang     │
  │         │ masih kosong → CVM masih terbuka.                                                               │
  ├─────────┼─────────────────────────────────────────────────────────────────────────────────────────────────┤
  │ #2      │ Register Turing agent → set LPGUARDIAN_TURING_AGENT_ID + signer (BACKEND_SIGNER_PK) di Railway, │
  │ anchor  │  lalu kirim recordTuringDecision: true. Tanpa ini, anchor skip graceful.                        │
  └─────────┴─────────────────────────────────────────────────────────────────────────────────────────────────┘

  Catatan: #2 & #1 butuh git push (Railway CI) + untuk #1, rebuild/redeploy CVM kalau mau auth aktif. Mau aku
  bantu rebuild+push image CVM lagi (sudah termasuk perubahan auth), atau commit perubahan Node dulu? Atau lanjut
  ke item lain?